# Boss Jump Attack Implementation

## 概要
ボスに新しいジャンプ攻撃を実装しました。ボスは定期的にプレイヤーの方向にジャンプして攻撃を行います。

## 実装内容

### 1. 新しい状態の追加
- `Boss::State`列挙型に`kJump`（ジャンプ攻撃）状態を追加

### 2. ジャンプ攻撃のパラメータ
以下のパラメータを`Boss.h`に追加しました：

- `kJumpSpeed = 0.4f` - ジャンプ時の初速度（上方向）
- `kJumpMoveSpeed = 0.1f` - ジャンプ中の水平移動速度
- `kJumpInterval = 4.5f` - ジャンプ攻撃の間隔（秒）
- `kGravity = 0.015f` - 重力加速度
- `kJumpRotation = 0.2f` - ジャンプ中の前傾姿勢の角度
- `jumpTimer_` - ジャンプ攻撃のタイミングを管理するタイマー
- `isOnGround_` - 地面にいるかどうかのフラグ
- `groundY_` - 地面のY座標を記憶

### 3. 実装された関数

#### `StartJump()`
- ジャンプ攻撃を開始する関数
- プレイヤーの位置を確認し、その方向に向かってジャンプ
- 上方向の速度と水平方向の速度を設定
- 状態を`kJump`に変更

#### `UpdateJump()`
- ジャンプ中の更新処理
- 重力を適用して徐々に落下
- 水平方向に移動
- 移動範囲の端に到達したら反転
- 地面に着地したらパトロール状態に戻る
- 空中では前傾姿勢（rotation.x = 0.2f）を取る

### 4. 状態遷移
- **パトロール状態**から：
  - `jumpTimer_`が`kJumpInterval`（4.5秒）を超えると`StartJump()`が呼ばれる
  - 地面にいる（`isOnGround_`がtrue）時のみジャンプ可能
  
- **ジャンプ状態**から：
  - 地面に着地すると自動的にパトロール状態に戻る

### 5. 既存機能との共存
- 突進攻撃（チャージ）とは独立したタイマーで管理
- 突進攻撃は3秒間隔、ジャンプ攻撃は4.5秒間隔
- どちらの攻撃も適切なタイミングで発動

## 動作説明

1. ボスは通常パトロール状態で左右に移動
2. 4.5秒ごとにプレイヤーの方向を確認
3. プレイヤーの方向に向かってジャンプ
4. ジャンプ中は放物線を描いて移動
5. 地面に着地するとパトロール状態に戻る

## テスト方法

ゲームを実行し、以下を確認してください：

1. ボスが定期的にジャンプすること
2. ジャンプの方向がプレイヤーの位置に応じて変わること
3. ジャンプ中も移動範囲の制限が守られること
4. 着地後、通常のパトロール動作に戻ること
5. 突進攻撃とジャンプ攻撃が両方適切なタイミングで発動すること

## パラメータ調整

必要に応じて以下のパラメータを調整できます：

- `kJumpSpeed` - 高さを調整（大きいほど高くジャンプ）
- `kJumpMoveSpeed` - 水平移動速度を調整
- `kJumpInterval` - ジャンプの頻度を調整（秒単位）
- `kGravity` - 落下速度を調整（大きいほど早く落ちる）
- `kJumpRotation` - ジャンプ中の姿勢角度を調整
